# safari-bookmarks-sorter
Sorts your Safari bookmarks

**Install dependencies**
```bash
gem install CFPropertyList
```

**Usage**

Create a backup copy of `~/Library/Safari/Bookmarks.plist` before running the script.

```bash
ruby sort.rb
```
